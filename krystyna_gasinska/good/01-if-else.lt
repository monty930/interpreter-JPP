Proc f(int y) {
    y++;
}

Proc main() {
    int x = 1;
    if (x >= 2) { f(&x); }
    else { f(x); }
    print_line((string) x);
    
    x = 2;
    if (x >= 2) { f(&x); }
    else { f(x); }
    print_line((string) x);
}
