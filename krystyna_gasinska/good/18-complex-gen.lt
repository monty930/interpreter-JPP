Gen int g1() {
    gen zmm = g2();
    yield(next(zmm));
}

Gen int g2() {
    gen zm = g1();
    yield(0);
    yield(next(zm));
}


Proc main () {
    for (i in g2()) {
        print_line("main: " + (string) i);
    }
}
